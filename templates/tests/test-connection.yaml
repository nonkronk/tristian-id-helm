apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tristian-id.fullname" . }}-test-connection"
  labels:
    {{- include "tristian-id.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tristian-id.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
